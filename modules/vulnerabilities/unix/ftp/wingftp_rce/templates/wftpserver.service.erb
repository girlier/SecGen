[Unit]
Description=Wing FTP Server
After=network.target

[Service]
Type=forking
WorkingDirectory=/opt/wftpserver
ExecStart=/opt/wftpserver/wftpserver
ExecStop=/bin/pkill -x wftpserver
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target